# Expo
.expo/
.expo-shared/
dist/
web-build/

# Native
android/
ios/
*.apk
*.aab
*.ipa
*.xcworkspace
*.xcodeproj
.gradle/

# Dependencies
node_modules/
yarn.lock
pnpm-lock.yaml
# package-lock.json IS committed (reproducible EAS/CI builds + pins the
# SDK-52-aligned versions so `npm install` can't silently drift
# react-native back to a version that breaks the native bridge).

# Environment
.env
.env.local
.env.*.local

# Misc
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
.vscode/
*.tsbuildinfo

# EAS
eas-build.log
